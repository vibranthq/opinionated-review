name: Build
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Build
        run: make
      - uses: actions/upload-artifact@master
        with:
          name: Artifacts
          path: ./dist
