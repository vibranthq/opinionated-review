name: Build
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Build PDF
        run: make epub
      - name: Build EPUB
        run: make epub
      - uses: actions/upload-artifact@master
        with:
          name: Artifacts
          path: ./dist
